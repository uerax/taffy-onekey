{
    "inbounds": [
        {
            "listen": "0.0.0.0",
            "port": "${port}",
            "tag": "${port}",
            "protocol": "vless",
            "key": "${pubicKey}",
            "settings": {
                "clients": [
                    {
                        "id": "${password}",
                        "flow": "xtls-rprx-vision"
                    }
                ],
                "decryption": "none"
            },
            "streamSettings": {
                "network": "tcp",
                "security": "reality",
                "realitySettings": {
                    "show": false,
                    "dest": "${reality_sni}:443",
                    "serverNames": [
                        "${reality_sni}"
                    ],
                    "privateKey": "${privateKey}",
                    "shortIds": [
                        "${shortId}",
                        ""
                    ]
                }
            }
        }
    ]
}